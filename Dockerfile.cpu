# ============================================================
# Dockerfile.cpu — CPU-only build (no CUDA required)
# ============================================================
# Slower (~5-15 min per image) but runs on any machine.
#
# Build:
#   docker build -f Dockerfile.cpu -t photoreal-engine:cpu .
#
# Run (CLI):
#   docker run -v $(pwd)/outputs:/app/outputs \
#     photoreal-engine:cpu python src/main.py "your prompt here"
#
# Run (API server):
#   docker run -p 8000:8000 \
#     -v $(pwd)/outputs:/app/outputs \
#     photoreal-engine:cpu
# ============================================================

FROM python:3.11-slim-bookworm

# ── System setup ──────────────────────────────────────────────
ENV DEBIAN_FRONTEND=noninteractive \
    PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1

RUN apt-get update && apt-get install -y --no-install-recommends \
        git \
        wget \
        curl \
        libgomp1 \
    && rm -rf /var/lib/apt/lists/*

# ── Python packages (CPU) ─────────────────────────────────────
RUN pip install --upgrade pip && \
    pip install torch==2.2.2 torchvision==0.17.2 \
        --index-url https://download.pytorch.org/whl/cpu

# Remaining deps
RUN pip install \
        diffusers==0.27.2 \
        transformers==4.40.1 \
        accelerate==0.29.3 \
        safetensors==0.4.3 \
        Pillow==10.3.0 \
        "numpy==1.26.4" \
        scipy==1.13.0 \
        fastapi==0.111.0 \
        "uvicorn[standard]==0.29.0" \
        pydantic==2.7.1 \
        huggingface-hub==0.23.0

# ── Application code ──────────────────────────────────────────
WORKDIR /app
COPY src/ ./src/
COPY entrypoint.sh ./entrypoint.sh

RUN chmod +x entrypoint.sh && \
    mkdir -p outputs

# ── Runtime configuration ─────────────────────────────────────
ENV HF_HOME=/app/.cache/huggingface \
    TRANSFORMERS_CACHE=/app/.cache/huggingface \
    HUGGINGFACE_HUB_CACHE=/app/.cache/huggingface \
    SD_MODEL=runwayml/stable-diffusion-v1-5 \
    PORT=8000

EXPOSE 8000

ENTRYPOINT ["./entrypoint.sh"]
CMD ["server"]

